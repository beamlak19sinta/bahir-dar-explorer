import DestinationsClient from './DestinationsClient';

export const metadata = {
  title: 'Destinations',
  description: 'Explore all must-visit destinations around Bahir Dar — Lake Tana, Blue Nile Falls, Bezawit Hill, ancient island monasteries, and more.',
};

export default function DestinationsPage() {
  return <DestinationsClient />;
}
