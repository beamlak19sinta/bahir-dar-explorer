import AttractionsClient from './AttractionsClient';

export const metadata = {
  title: 'Attractions',
  description: 'Browse all top attractions around Bahir Dar — waterfalls, monasteries, scenic viewpoints, local markets, and more.',
};

export default function AttractionsPage() {
  return <AttractionsClient />;
}
